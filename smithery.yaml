build:
  dockerfile: Dockerfile
startCommand:
  type: stdio
  configSchema:
    type: object
    properties:
      fridayUrl:
        type: string
        default: "http://localhost:8000"
        description: "Friday Memory Gateway URL"
      fridayApiKey:
        type: string
        default: "change_this_to_your_secret_key"
        description: "Friday Brain API Key"
  commandFunction: |-
    (config) => ({
      command: "python",
      args: ["-m", "mcp.server"],
      env: {
        FRIDAY_URL: config?.fridayUrl || "http://localhost:8000",
        FRIDAY_API_KEY: config?.fridayApiKey || "change_this_to_your_secret_key",
        BRAIN_API_KEY: config?.fridayApiKey || "change_this_to_your_secret_key"
      }
    })
